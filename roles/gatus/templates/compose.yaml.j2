---
services:
  gatus:
    image: ghcr.io/twin/gatus:v5.6.0
    container_name: gatus
{% if gatus_healthchecks_network is defined %}
    networks:
      - {{ gatus_healthchecks_network }}
{% endif %}
    ports:
      - 8080:8080
    volumes:
      - ./config.yaml:/config/config.yaml
    restart: unless-stopped

{% if gatus_healthchecks_network is defined %}
networks:
  {{ gatus_healthchecks_network }}:
    external: true
{% endif %}
